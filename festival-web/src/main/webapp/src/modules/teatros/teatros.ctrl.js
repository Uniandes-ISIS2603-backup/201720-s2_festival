(function (ng) {
   var mod = ng.module("teatrosModule");
    mod.controller("teatrosCtrl", ["$scope", "$http", "teatrosContext","$state",
        function($scope, $http, teatrosContext,$state){
            
        }
    ]);
}
)(angular);



